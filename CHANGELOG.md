# 更新日志 (Changelog)

## [未发布]

### 新增
- 遥控器上键/下键功能可自定义：在「设置 → 播放与视频」中可循环选择 音频轨道 / 字幕选择 / 弹幕设置 / 视频轨道 / 倍速 / 视频比例（默认上键=弹幕设置、下键=音轨选择）
- 字幕「距离底部位置」支持负数：最低可调到 -100 dp
- 文件浏览器右侧预览区支持直接预览图片文件，覆盖本地文件 / SMB / FTP / NFS / WebDAV / HTTP 链接全部数据源

### 优化
- 字幕设置中的「字体大小」「距离底部位置」改为加减号数字调节控件（与弹幕设置一致），替代原来的弹窗列表，调节更直观
- 图片查看器新增焦点管理：控制栏显示时焦点位于控制栏，隐藏时焦点回到图片区域，遥控器操作更顺手
- 图片加载统一复用同一套带远程协议支持的 ImageLoader，图片查看器与文件列表预览行为一致
- 下调 VLC 播放缓存：本地文件 500ms → 200ms、网络协议 200ms → 100ms，起播更快
- 依赖升级：Compose BOM 2026.08.00 → 2026.09.00、libvlc 3.7.1 → 3.7.5、media3 1.11.0 → 1.11.1、Room 2.8.4 → 2.8.5、Compose UI 1.12.0 → 1.12.1

### 修复
- 修复字幕底边距设为负数时崩溃（Padding must be non-negative），负数改用 offset 实现
- 修复首次启动焦点落到导航栏、导致导航栏自动展开、按返回键直接退回桌面的问题；现在导航栏展开时按返回键会先收起导航栏
- 修复图片查看器控制栏展开时按返回键直接退出页面的问题，现在按返回键会先收起控制栏
- 修复文件浏览器右侧预览区选中图片时不显示图片的问题：图片预览改用带 RemoteMediaFetcher 的 ImageLoader 并传入数据源类型，同时图片文件不再被同名电影的刮削海报覆盖
- 修复 NFS 连接失败提示文案多出一层引号的问题
