*.iml
.gradle
/local.properties
.idea/
.DS_Store
# 各 module 自己的 build/ 也要忽略（`:core` 与 `:app` 各有一份，只写 `/build` 会漏掉 `core/build`）
build/
/captures
.externalNativeBuild
.cxx
.artifacts
local.properties
/app/release/
gradle/gradle-daemon-jvm.properties
.workbuddy/

# 签名密钥：只放在本地或 GitHub Secrets 里，绝不入库
*.jks
*.keystore
keystore/
release.jks
