<?xml version="1.0" encoding="utf-8"?>
<!--
    只放两个 flavor 共用的东西：权限 + <application> 的通用属性。
    Activity / banner / theme 都各自下沉到 src/tv 与 src/phone 的清单里
    —— 这样 tv 变体不会看见手机端的 Activity，手机变体也不会看见电视端的入口。

    注意：application-id 也按 flavor 拆开（电视端 org.mz.mzdkplayer / 手机端 org.mz.mzdkplayer.phone），
    两个 App 可以同时装在同一台设备上，天生不存在「桌面两个图标」的问题。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 外部存储读取权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="AllFilesAccessPolicy,ScopedStorage" /><!--add 2022-2-11 Android11-->
    <!-- 外部存储写入权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Android 13+ 媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 13+ 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 14 部分访问权限 (如果需要) -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <!--  android:largeHeap="true" 增加Android应用的堆内存限制 -->
    <application
        android:allowBackup="true"
        android:name=".MzDkPlayerApplication"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:windowSoftInputMode="stateAlwaysHidden"
        android:label="@string/app_name"
        android:supportsRtl="true" />
</manifest>
